<nav class="bg-white dark:bg-gray-800 shadow-md py-4 px-6 flex justify-between items-center">
  <div class="text-xl font-extrabold text-blue-600 dark:text-blue-400">
    <%= link_to 'Home', root_path, class: "hover:text-blue-700 dark:hover:text-blue-500" %>
  </div>

  <div class="flex space-x-4 items-center">
    <% if user_signed_in? %>
      <span class="text-gray-600 dark:text-gray-300 font-medium mr-4"><%= current_user.email %></span>
      <%= link_to 'Articles', articles_path, class: "text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 font-medium" %>
      <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: "text-gray-600 dark:text-gray-300 hover:text-red-600 dark:hover:text-red-400 font-medium" %>
    <% else %>
      <%= link_to 'Login', new_user_session_path, class: "text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 font-medium" %>
    <% end %>
  </div>
</nav>
